name: 'Actualizador Clumsex (10 min)'

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  auto_update:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      
    steps:
      - name: Checkout del Repositorio
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar Librería de Google
        run: pip install google-genai

      # El script ahora maneja sus propios reintentos internamente
      - name: Ejecutar Bot Auto-Curativo
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python review_bot.py

      - name: Commit y Push Robusto
        run: |
          git config --global user.name 'Gemini Bot'
          git config --global user.email 'bot@noreply.github.com'
          
          git add -f clumsex.py CHANGELOG.md TODO.md
          
          git commit -m "chore: [AUTO-UPDATE] Código + Changelog" || echo "⚠️ Sin cambios"
          
          # Estrategia de fusión segura (gana lo que esté en GitHub si hay conflicto)
          git pull --no-rebase -s recursive -X ours origin main
          
          git push origin HEAD:main

      # === NOTIFICACIÓN DISCORD ===
      - name: Notificar a Discord
        if: success() 
        uses: sarisia/actions-status-discord@v1
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK }}
          title: "🚀 Gemini Bot: ¡Update Exitoso!"
          description: "Se ha optimizado el código y actualizado el Changelog."
          color: 0x00FF00
          username: Gemini Bot
